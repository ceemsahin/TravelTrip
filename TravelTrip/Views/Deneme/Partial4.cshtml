@using TravelTrip.Models.Siniflar
@model List<Blog>
<div class="padding1">
    <h4>En iyi yerler</h4>
    <div class="box-left">
        @foreach (var x in Model)
        {


            <div class="bot-img">
                <img src="@x.BlogImage" alt="" />
            </div>
            <div class="bot-desc">
                <p> @x.Aciklama.Substring(0, 50) </p>
            </div>

            <div class="clear"></div>
        }
    </div>

    <div class="box1-left">


        @Html.Action("Partial5", "Deneme")


    </div>


</div>